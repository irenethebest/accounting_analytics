# Screenshots

Put PNG/GIF captures here so they render in the top-level README:

- `pipeline.png` — the Databricks Workflow job graph (`ingest_bronze → build_gold`)
- `cicd.png` — a green GitHub Actions run (validate → deploy)
- `dashboard.png` — the running app (FP&A / Anomaly / Recon tabs)

Tip: a short animated GIF of clicking through the three tabs makes a great hero image.
