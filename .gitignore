# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.ipynb_checkpoints/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs / temp
*.log
*.tmp

# Databricks
.databricks/

# --- Secrets / credentials: NEVER commit these ---
.databrickscfg
.databricks-cfg
*.env
.env
.env.*
secrets.*
*.secret
*.pem
*.key
*.p12
*.pfx
credentials.json
service-account*.json
*token*.txt
auth.toml

# Regenerable intermediate: generate_data.py recreates data/ on demand, and the
# Databricks pipeline generates in memory. The committed deliverable is
# app/dashboard_data.js, so the raw CSVs don't need to be in the repo.
data/
